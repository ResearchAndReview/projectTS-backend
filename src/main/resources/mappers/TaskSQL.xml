<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="TaskMapper">
    <select id="findAll" resultType="Task">
        SELECT * FROM Task;
    </select>
    <insert id="insertOne">
        INSERT INTO Task (working_node_id, status, created_at, updated_at, fail_cause, translate_from, translate_to)
        VALUES (null, DEFAULT, DEFAULT, DEFAULT, null, #{translateFrom}, #{translateTo});
    </insert>
   <!-- <resultMap id="userResultMap" type="com.example.myapp.model.User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
    </resultMap>-->

    <!--<select id="findById" parameterType="long" resultMap="userResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="userResultMap">
        SELECT * FROM users
    </select>

    <insert id="insert" parameterType="com.example.myapp.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (name, email) VALUES (#{name}, #{email})
    </insert>

    <update id="update" parameterType="com.example.myapp.model.User">
        UPDATE users SET name=#{name}, email=#{email} WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM users WHERE id=#{id}
    </delete>-->
</mapper>
